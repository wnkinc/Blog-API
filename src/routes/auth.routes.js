const express = require("express");
const router = express.Router();

router.post("/login" /* Controller Logic */);
router.post("/signup" /* Controller Logic */);
router.post("/logout" /* Controller Logic */);

module.exports = router;
